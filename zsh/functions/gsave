# Save git progress without OVERCOMMIT complaining
#
#   gsave

function gsave() {
  export OVERCOMMIT_DISABLE=1
  echo "OD = ${OVERCOMMIT_DISABLE}"
  git add -A
  git commit -am "SAVEPOINT"
  git push
}
