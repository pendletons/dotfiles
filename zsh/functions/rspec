# Run system/feature specs
#
#   brs

function brs() {
  export TEST_TYPE=system
  bin/rspec --tag type:system --tag type:feature
}

# Run unit specs
#
#   brs

function brs() {
  export TEST_TYPE=system
  bin/rspec --tag type:system
}
